# MongoDB-No-SQL-
**MongoDB (NoSQL)** is a document-based database that stores data in flexible, JSON-like documents instead of tables. It is scalable, schema-less, and ideal for handling large and unstructured data.
